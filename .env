# .env example for ZFQ (Zero Friction Query)

# Path to the SQLite database (ensure this file exists or is created dynamically)
DATABASE_PATH=/path/to/your/database.sqlite3

# Database connection name (optional, if you want to work with multiple connections)
DB_CONNECTION_NAME=test_db

# List of all registered databases (space-separated values)
# This could be populated dynamically from the user's environment or pre-defined
REGISTERED_DATABASES="test_db production_db"

# Default table to be used when no table is selected (optional)
DEFAULT_TABLE=users

# User-selected table (for simulation purposes, can be adjusted dynamically)
SELECTED_TABLE=users

# Columns to display by default (comma-separated)
DEFAULT_COLUMNS="id,name,email,age"

# Sorting preferences (default column to sort by)
DEFAULT_SORT_COLUMN=age
DEFAULT_SORT_ORDER=asc  # Use 'desc' for descending order

# Row limit for default display
DEFAULT_ROW_LIMIT=10

# Column widths (set manually or dynamically for table formatting)
COLUMN_WIDTHS="id:5,name:15,email:25,age:5"

# Enable or disable interactive mode (set to 1 for true, 0 for false)
INTERACTIVE_MODE=1

# Debug mode (set to 1 to enable debug output, 0 to disable)
DEBUG_MODE=0

# Connection status (simulated, not actually connected to a real DB in this prototype)
DB_CONNECTED=1

# Enable dramatic effect for output (pause between rows)
DRAMATIC_EFFECT=1

# Sleep time for dramatic effect (in seconds)
SLEEP_TIME=0.4
